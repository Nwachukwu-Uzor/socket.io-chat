import { createTheme } from "@mui/material/styles";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#facc15",
      light: "#fef08a",
      contrastText: "#fff",
      dark: "#facc15",
    },
    secondary: {
      main: "#16a34a",
      light: "#86efac",
      contrastText: "#fff",
    },
  },
});
